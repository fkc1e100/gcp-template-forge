apiVersion: v1
kind: Service
metadata:
  name: audit-service
  labels:
    app: audit-workload
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: audit-workload
