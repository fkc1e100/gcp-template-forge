apiVersion: v1
kind: Service
metadata:
  name: {{ include "consolidate-duplicat-workload.fullname" . }}
  labels:
    {{- include "consolidate-duplicat-workload.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "consolidate-duplicat-workload.selectorLabels" . | nindent 4 }}
