apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: enterprise-secrets
  namespace: default
spec:
  provider: gcp
  parameters:
    secrets: |
      - resourceName: "projects/gca-gke-2025/secrets/enterprise-workload-secret/versions/latest"
        fileName: "enterprise-workload-secret"
