apiVersion: v1
kind: Service
metadata:
  name: hello-custom-compute
  labels:
    app: hello-custom-compute
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: hello-custom-compute
