apiVersion: v1
kind: Service
metadata:
  name: custom-workload
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
  selector:
    app: custom-workload
