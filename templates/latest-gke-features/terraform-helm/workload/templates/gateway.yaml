{{- if .Values.gateway.enabled }}
kind: Gateway
apiVersion: gateway.networking.k8s.io/v1
metadata:
  name: {{ .Values.gateway.name }}
spec:
  gatewayClassName: gke-l7-gclb
  listeners:
  - name: http
    protocol: HTTP
    port: 80
{{- end }}
