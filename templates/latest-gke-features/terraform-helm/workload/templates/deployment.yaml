apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "latest-features.fullname" . }}
  labels:
    app: {{ include "latest-features.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "latest-features.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "latest-features.name" . }}
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 2000
      # LATEST FEATURE: Native Sidecar Containers (requires K8s 1.29+)
      initContainers:
      - name: logger-sidecar
        image: {{ .Values.sidecar.image }}:{{ .Values.sidecar.tag }}
        restartPolicy: Always
        command: ["sh", "-c", "while true; do echo \"$(date) - heartbeat from native sidecar\" >> /var/log/app.log; sleep 30; done"]
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          privileged: false
        resources:
          {{- toYaml .Values.sidecarResources | nindent 10 }}
        volumeMounts:
        - name: logs
          mountPath: /var/log
      
      containers:
      - name: web-app
        image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          privileged: false
        ports:
        - name: http
          containerPort: 80
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
        volumeMounts:
        - name: logs
          mountPath: /var/log
      
      volumes:
      - name: logs
        emptyDir:
          sizeLimit: 1Gi

      # LATEST FEATURE: Pod Topology Spread Constraints
      topologySpreadConstraints:
      - maxSkew: 1
        topologyKey: kubernetes.io/hostname
        whenUnsatisfiable: ScheduleAnyway
        labelSelector:
          matchLabels:
            app: {{ include "latest-features.name" . }}
      - maxSkew: 1
        topologyKey: topology.kubernetes.io/zone
        whenUnsatisfiable: ScheduleAnyway
        labelSelector:
          matchLabels:
            app: {{ include "latest-features.name" . }}
