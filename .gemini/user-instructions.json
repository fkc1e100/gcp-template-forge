{
  "repository_objective": "Generate, test, deploy, and validate IaC templates for full-stack application environments on GCP.",
  "tech_stack": [
    "Terraform",
    "Helm",
    "Config Connector"
  ],
  "guidelines": [
    "Support dual-path IaC generation: Terraform/Helm and Config Connector.",
    "For every issue requiring infrastructure generation, the agent MUST create two distinct folders under templates/<issue_number>-<description>/: one named kcc/ for Config Connector manifests, and one named terraform/ for Terraform/Helm files.",
    "All generated cloud resources (clusters, networks, buckets) MUST include the issue number in their name (e.g., cluster-issue-6 or bucket-issue-6) to ensure uniqueness.",
    "Do not use hardcoded project IDs or generic placeholders like YOUR_PROJECT_ID in templates. Instead, use variable interpolation for Terraform, and clearly documented environment variable placeholders (e.g., ${PROJECT_ID}) for KCC manifests.",
    "Focus on aggregating and validating public reference architectures (refer to README.md for listed repositories).",
    "Ensure templates are security scanned and work out-of-the-box.",
    "Use the example repositories listed in the README.md as sources of truth and options for deployments.",
    "Consider and factor in new GKE features if the example architectures could benefit from them.",
    "When selecting GKE release channels: Use RAPID for templates requiring bleeding-edge features or AI/ML workloads. Use REGULAR or STABLE for standard enterprise templates to ensure production maturity."
  ],
  "success_criteria": {
    "definition": "A successful issue resolution involves a successful deployment of a TF/Helm based cluster/workload, as well as a completely separate successful deployment of a KCC managed deployment.",
    "requirements": [
      "Cluster and workloads are deployed.",
      "Workload has an endpoint that is accessible (either via Public IP or verifiable via internal cluster checks like kubectl exec from a test pod if the cluster is private).",
      "Every PR generated by the agent MUST include a verification_plan.md in the template folder, detailing the exact commands to run to deploy and verify both the KCC and TF/Helm paths."
    ],
    "failure_handling": "If deployments fail, the agent should look at failures, research options, and work to resolve/fix, always looking back at the ask in the original issue."
  }
}
