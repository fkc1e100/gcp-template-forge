version: "3.8"
services:
  web:
    build: .
    ports:
      - "80:80"
    depends_on:
      - api
    restart: always

  api:
    build:
      context: .
      dockerfile: api/Dockerfile
    ports:
      - "5000:5000"
    environment:
      - FLASK_APP=api/progress.py
    restart: always
